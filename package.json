{
  "name": "dxcore",
  "private": true,
  "packageManager": "pnpm@10.18.3",
  "scripts": {
    "deps": "turbo run deps",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
