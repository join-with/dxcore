{
  "name": "dxcore-agents-cli",
  "version": "0.1.0",
  "private": true,
  "description": "DxCore agents CLI - agent, dispatch, ci subcommands",
  "scripts": {
    "clean-deps": "rm -rf deps",
    "deps": "mix deps.get",
    "build": "mix compile --warnings-as-errors",
    "test": "mix test",
    "lint": "mix format --check-formatted",
    "format": "mix format"
  }
}
